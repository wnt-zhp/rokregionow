{% extends '::base.html.twig' %}

{% block body %}
  <form action="{{ path('team_update', {'slug': team.getShortName}) }}" method="POST" {{ form_enctype(form) }}>
      {{ form_widget(form) }}

      <input type="submit" />
  </form>
{% endblock %}

